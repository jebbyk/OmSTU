
<table class="table">
    <tr>
        <td>
            <h3>user</h3>
        </td>
    </tr>

    @foreach (var item in ViewBag.result)
    {
        <tr>
            <td>
                @Html.ActionLink((string)item.name, "Details", "users", new { id = item.id }, null)
            </td>
        </tr>
    }
</table>
